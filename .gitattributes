# Держим LF в Go-исходниках и module-файлах, чтобы gofmt / `make fmt`
# не создавали CRLF<->LF шум при checkout на Windows (core.autocrlf=true).
*.go    text eol=lf
go.mod  text eol=lf
go.sum  text eol=lf
